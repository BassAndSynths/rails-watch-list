
<div class="navbar-lewagon text-center">
  <h1> Display Movies to the page</h1>
  <%= link_to "Create a new list", "/lists/new", class: "button" %>
</div>


<div class="container">
  <div class="card-holder">
  <% @list.each do |list| %>
  <%= link_to list_path(list) do %>
    <div class="movie-card text-center align-content-center">
      <p> <%= list.name%></p>
    </div>
  <% end %>
  <% end %>
</div>



</div>

<%# style="background-image: url(<%=list[0].poster_url%>
